{
    "version": 2,
    "installCommand": "npx @puppeteer/browsers install chrome@116.0.5793.0",
    "builds": [
        {
            "src": "index.js",
            "use": "@vercel/node",
            "config": {
                "build": {
                    "commands": [
                        "npm install",
                        "npm run install-chrome"
                    ]
                }
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "index.js"
        }
    ],
    "crons": [
        {
            "path": "/scrape",
            "schedule": "0 11 * * *"
        }
    ]
}